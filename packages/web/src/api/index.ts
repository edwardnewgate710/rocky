/**
 * `@chess-platform/web` typed API layer — request/response models plus the
 * {@link GambitClient} that consumes the approved M4 REST contract.
 */
export * from './models.js';
export * from './client.js';
